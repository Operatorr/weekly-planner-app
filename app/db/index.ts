/**
 * Database layer placeholder.
 *
 * This module will be set up in T02 to provide:
 * - Neon PostgreSQL client via @neondatabase/serverless
 * - Query helpers for tasks, projects, checklist items, reminders, etc.
 *
 * The actual DB connection is used from Vercel serverless API routes (api/),
 * not from the client-side SPA.
 */

export {};
